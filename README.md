# WorkenAI — AI Ассистент с Generative UI и интеграцией Excel

WorkenAI — это упрощённый аналог интерфейса ChatGPT с поддержкой тредов, хранением истории сообщений и **Generative UI** на базе Vercel AI SDK. Приложение позволяет работать с Excel-таблицами напрямую из чата, поддерживая чтение и запись с подтверждением опасных действий.

## 🛠 Технологический стек

- **Framework**: Next.js 16 (App Router, TypeScript)
- **AI SDK**: Vercel AI SDK (Core, UI, Google Provider)
- **Model**: Google Gemini 2.5 Flash (через `@ai-sdk/google`)
- **Runtime**: Bun 1.3+
- **Database**: SQLite (встроенный модуль `bun:sqlite`)
- **Styling**: Tailwind CSS + Lucide React
- **XLSX**: Библиотека `xlsx` для работы с Excel
- **Markdown**: `react-markdown` + `remark-gfm`

## ✨ Реализованный функционал

### 1. Чат с тредами (как в ChatGPT)

- **Список диалогов**: Слева отображается история тредов. Можно создавать новые и переключаться между существующими.
- **Персистентность**: Все сообщения и треды сохраняются в локальной базе данных SQLite (`data/sqlite.db`).
- **Интерфейс**: Реализован на базе `useChat` из AI SDK UI со стримингом ответов.

### 2. Generative UI (Клиентские инструменты)

- **Интерактивные компоненты**: Модель может вызывать инструменты, которые рендерят UI прямо в чате (например, таблицы или карточки подтверждения).
- **Подтверждение действий**: Критические операции (запись в файл) требуют явного подтверждения пользователя через UI («Да» / «Нет»).

### 3. Интеграция с Excel (XLSX)

- **Чтение данных**: Агент может читать данные из файла `data/example.xlsx` по запросу (например, "Покажи данные с листа Sheet1").
- **Запись данных**: Агент может изменять данные в ячейках (инструмент `writeCell`).
- **Подтверждение записи**: Любая попытка изменить файл вызывает tool `confirmAction`, отображающий карточку с деталями изменения. Запись происходит только после нажатия кнопки "Да, подтвердить".
- **Просмотр таблицы**: При получении таблицы можно открыть её в модальном окне на весь экран.
- **Выделение и меншоны**: В модальном окне можно выделить ячейки, и они добавятся в поле ввода как ссылка (например, `@Sheet1!A1:B3`), которую понимает агент.

## 🚀 Как запустить проект

### Требования

- Установленный **Bun** (версии 1.3 или выше).
- Ключ API Google Generative AI (Gemini).

### Установка и запуск

1. **Клонируйте репозиторий** и перейдите в папку проекта.

2. **Установите зависимости**:

   ```bash
   bun install
   ```

3. **Настройте переменные окружения**:
   Создайте файл `.env.local` в корне проекта и добавьте ваш ключ:

   ```env
   GOOGLE_GENERATIVE_AI_API_KEY=ваш_ключ_здесь
   ```

4. **Подготовьте данные**:
   Убедитесь, что в папке `data/` есть файл `example.xlsx` (или создайте/положите свой). Если папки нет, она создастся автоматически при инициализации БД, но Excel файл нужен для тестов таблицы.

   _Примечание: Можно использовать скрипт `bun run scripts/create-xlsx.ts` (если он есть) или просто создать файл в Excel._

5. **Запустите сервер разработки**:

   ```bash
   bun dev
   ```

6. **Откройте приложение**:
   Перейдите по адресу [http://localhost:3000](http://localhost:3000).

## 🧪 Примеры использования (Как тестировать)

### Тест 1: Обычный чат

- Напишите: _"Привет, кто ты?"_
- Агент должен ответить на русском языке, представившись как WorkenAI.

### Тест 2: Чтение таблицы

- Убедитесь, что `data/example.xlsx` существует.
- Напишите: _"Прочитай данные с листа Sheet1"_ (или другое имя листа в вашем файле).
- Агент должен отобразить мини-превью таблицы.
- Нажмите кнопку разворачивания (иконка максимизации) в заголовке таблицы, чтобы открыть модальное окно.

### Тест 3: Меншоны (Mentions)

- В открытом модальном окне выделите несколько ячеек мышью.
- Ссылка на диапазон (например, `@Sheet1!A1:B5`) автоматически добавится в поле ввода.
- Допишите вопрос: _"Посчитай сумму в этом диапазоне @Sheet1!A1:B5"_ и отправьте.
- Агент должен понять контекст диапазона.

### Тест 4: Запись с подтверждением

- Напишите запрос на изменение: _"Запиши 'Тест' в ячейку C5 на листе Sheet1"_.
- Агент **не** должен менять файл сразу. Вместо этого появится карточка подтверждения: _"Обновить C5 на 'Тест'?"_.
- Нажмите **"Отмена"** — в чате появится сообщение об отмене.
- Повторите запрос и нажмите **"Да, подтвердить"**.
- Появится сообщение об успешном обновлении, и файл реально обновится.

## 📂 Структура проекта

- `src/app/api/chat`: API route для чата (Server-side логика и определения tools).
- `src/components/chat`: UI компоненты чата (`ChatInput`, `ChatMessage`, `ConfirmActionCard`).
- `src/lib/xlsx`: Логика работы с xlsx файлами через библиотеку `xlsx`.
- `src/db`: Инициализация SQLite и методы работы с БД.
